"use client";

import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const Row = styled(Box)(() => ({
  display: "flex",
  flexDirection: "row",
}));

export default Row;
